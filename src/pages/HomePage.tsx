import React, { FC } from "react";
import CardsGrid from "../components/cards/CardsGrid";

const HomePage: FC = () => {
  return (
    <div>
      <CardsGrid />
    </div>
  );
};

export default HomePage;
